import React from "react";

const Footer = () => (
  <footer className="bg-light p-3 text-center">
    <div className="logo" />
    <p>
      ElasticSearch Web App - Consuming API Module via <a href="#">MuleSoft Anypoint Plateform</a>
    </p>
  </footer>
);

export default Footer;
